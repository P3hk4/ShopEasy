# Используем официальный образ PostgreSQL (лучше указать конкретную версию)
FROM postgres:15-alpine

# Открываем порт PostgreSQL (чисто декларативно, реальное пробрасывание делается в compose)
EXPOSE 5432